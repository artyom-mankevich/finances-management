import { CurrencyLocationPipe } from './currency-location.pipe';

describe('CurrencyLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
